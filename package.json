{
  "name": "joyreactor-ux",
  "version": "0.1.8",
  "scripts": {
    "build": "tsc && vite build",
    "build:extension": "npm run build && web-ext build",
    "watch": "tsc --noEmit && vite build --watch"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.0",
    "web-ext": "^9.2.0"
  }
}
